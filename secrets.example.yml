################################################################################
# This file contains secrets that should not be checked into VCS.
################################################################################
---
secrets:
  jupyter_password: SuperSecretPassword
  sync:
    source: /nfs/data
    destination: /nfs
  users:
    - ansible
  volumes:
    - /nfs/src:/notebooks
