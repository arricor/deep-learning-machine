################################################################################
# This file contains secrets that should not be checked into VCS.
################################################################################
---
secrets:
  sync:
    source: /nfs/data
    destination: /nfs
  users:
    - ansible
  volumes:
    data: /nfs/data:/data
    source: /nfs/src:/tf
